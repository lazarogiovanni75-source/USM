{
  "deploy": {
    "startCommand": "./bin/railway-boot",
    "preDeployCommand": "rails db:migrate && bin/rails server",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
