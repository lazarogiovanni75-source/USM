<%= turbo_stream.replace "chat-messages", partial: "chat_messages", locals: { conversation: @conversation, messages: @conversation.ai_messages } %>
<%= turbo_stream.replace "chat-form", partial: "chat_form", locals: { conversation: @conversation } %>
