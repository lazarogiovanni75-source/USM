require 'rails_helper'

RSpec.describe BufferAnalytic, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:buffer_analytic)
  #   expect(record).to be_persisted
  # end
end
