require 'rails_helper'

RSpec.describe PromptTemplate, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:prompt_template)
  #   expect(record).to be_persisted
  # end
end
