require 'rails_helper'

RSpec.describe PerformanceMetric, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:performance_metric)
  #   expect(record).to be_persisted
  # end
end
