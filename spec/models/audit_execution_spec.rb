require 'rails_helper'

RSpec.describe AuditExecution, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:audit_execution)
  #   expect(record).to be_persisted
  # end
end
