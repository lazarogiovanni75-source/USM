require 'rails_helper'

RSpec.describe StrategyHistory, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:strategy_history)
  #   expect(record).to be_persisted
  # end
end
