require 'rails_helper'

RSpec.describe UserSubscription, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:user_subscription)
  #   expect(record).to be_persisted
  # end
end
