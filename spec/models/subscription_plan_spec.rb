require 'rails_helper'

RSpec.describe SubscriptionPlan, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:subscription_plan)
  #   expect(record).to be_persisted
  # end
end
