require 'rails_helper'

RSpec.describe VoiceCommand, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:voice_command)
  #   expect(record).to be_persisted
  # end
end
