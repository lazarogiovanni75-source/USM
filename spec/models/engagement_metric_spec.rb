require 'rails_helper'

RSpec.describe EngagementMetric, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:engagement_metric)
  #   expect(record).to be_persisted
  # end
end
