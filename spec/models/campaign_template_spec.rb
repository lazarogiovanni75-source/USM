require 'rails_helper'

RSpec.describe CampaignTemplate, type: :model do
  # Example: Basic factory test
  # it 'can be created with factory' do
  #   record = create(:campaign_template)
  #   expect(record).to be_persisted
  # end
end
